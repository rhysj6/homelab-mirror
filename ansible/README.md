# Ansible
I use Ansible to automate the configuration and maintenance of servers in my homelab. Whilst I use Terraform for provisioning infrastructure and kubernetes workloads, Ansible is my go-to for any configuration management tasks on my VMs and bare metal servers. I have playbooks for setting up new servers, and deploying applications that don't run in Kubernetes.

